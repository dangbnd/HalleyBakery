Multi-tab products:

- Có thể cấu hình qua `Admin > Cấu hình > Product Tabs (GID:Key)`.
- Hoặc cấu hình qua `.env`:
  `VITE_PRODUCT_TABS=gid:key,gid2:key2`
- Định dạng: `GID:TênDanhMục`, phân tách bằng `,` hoặc xuống dòng.
